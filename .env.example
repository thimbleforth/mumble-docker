# Environment variables for Mumble Docker container

# Timezone (default: UTC)
TZ=UTC

# =============================================================================
# Server Configuration Variables
# =============================================================================
# NOTE: The variables below are for REFERENCE ONLY and are NOT automatically
# applied to the Mumble server configuration. To use these settings, you must
# manually configure them in /etc/mumble-server/mumble-server.ini inside the
# container or via a volume-mounted configuration file.
#
# These example values show recommended settings that can be set in
# mumble-server.ini for production use.
# =============================================================================

# Maximum number of users
MUMBLE_MAX_USERS=50

# Server password (leave empty for no password)
MUMBLE_SERVER_PASSWORD=

# Server name
MUMBLE_SERVER_NAME=Mumble Server

# Welcome message
MUMBLE_WELCOME_TEXT=Welcome to this Mumble server!

# Port (default: 64738)
MUMBLE_PORT=64738

# Maximum bandwidth per client (in bits per second)
# Recommended: 40000-90000 (5-11.25 KB/s)
MUMBLE_BANDWIDTH=90000

# Resource Limits
MUMBLE_CPU_LIMIT=2
MUMBLE_MEMORY_LIMIT=512M
MUMBLE_CPU_RESERVATION=0.5
MUMBLE_MEMORY_RESERVATION=128M
